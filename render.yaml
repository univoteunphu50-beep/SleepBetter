services:
  # Aplicación web SleepBetter
  - type: web
    name: sleepbetter-app
    env: docker
    plan: free
    region: oregon
    buildCommand: docker build -t sleepbetter .
    startCommand: docker run -p $PORT:80 sleepbetter
    envVars:
      - key: PORT
        value: 10000 